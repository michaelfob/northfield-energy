const bill=document.getElementById('monthlyBill'),billDisplay=document.getElementById('billDisplay'),type=document.getElementById('businessType'),annualSaving=document.getElementById('annualSaving'),savingRate=document.getElementById('savingRate'),co2=document.getElementById('co2');function money(n){return '£'+Math.round(n).toLocaleString('en-GB')}function updateCalc(){const m=Number(bill.value),r=Number(type.value),s=m*12*r;billDisplay.textContent=money(m);annualSaving.textContent=money(s);savingRate.textContent=Math.round(r*100)+'%';co2.textContent=Math.max(8,Math.round(s/500))+' tonnes'}bill?.addEventListener('input',updateCalc);type?.addEventListener('change',updateCalc);updateCalc();const checks=document.querySelectorAll('.eligible-check'),result=document.getElementById('checkerResult');function updateEligibility(){const all=[...checks].every(c=>c.checked);result.textContent=all?'Your business appears suitable for a free funding assessment.':'Tick all four boxes to check likely eligibility.';result.classList.toggle('good',all)}checks.forEach(c=>c.addEventListener('change',updateEligibility));